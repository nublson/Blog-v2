import styled from 'styled-components'

export const Container = styled.div`
    text-align: center;
    margin-top: 18rem;
    font-size: 1.3rem;

    color: var(--text-highlight);

    a {
        color: var(--primary);
        font-weight: 600;
    }
`
